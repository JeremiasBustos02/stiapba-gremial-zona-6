import { useQuery } from '@tanstack/react-query'
import { AlertCircle, ChevronLeft, ChevronRight, Download, FileText, LoaderCircle, Search, SlidersHorizontal, X } from 'lucide-react'
import { useDeferredValue, useEffect, useState } from 'react'
import { filenameFromHeaders, getDocumentHistory, regenerateDocument, type DocumentHistoryFilters, type DocumentHistoryRecord } from '@/features/documents/documentsApi'
import { DisabledEmailAction } from '@/features/documents/DisabledEmailAction'
import { downloadDocument } from '@/features/documents/documentActions'
import { ApiError } from '@/lib/api'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { ActionGroup, ActionGroupItem } from '@/components/ui/action-group'
import { Dialog, DialogClose, DialogContent, DialogDescription, DialogHeader, DialogTitle } from '@/components/ui/dialog'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Select } from '@/components/ui/select'
import { Skeleton } from '@/components/ui/skeleton'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'

const documentTypeLabels: Record<string, string> = { PERMISO_GREMIAL: 'Permiso gremial' }
const emptyFilters: DocumentHistoryFilters = { q: '', issueDateFrom: '', issueDateTo: '', createdBy: '', order: 'newest' }

export function HistoryPage({ role }: { role: 'ADMIN' | 'DELEGADO' }) {
  const [page, setPage] = useState(0)
  const [filters, setFilters] = useState<DocumentHistoryFilters>(emptyFilters)
  const [filtersOpen, setFiltersOpen] = useState(false)
  const deferredQuery = useDeferredValue(filters.q ?? '')
  const [selected, setSelected] = useState<DocumentHistoryRecord | null>(null)
  const [downloadingId, setDownloadingId] = useState<string | null>(null)
  const [downloadError, setDownloadError] = useState('')
  const requestFilters = { ...filters, q: deferredQuery }
  const hasFilters = Boolean(filters.q || filters.issueDateFrom || filters.issueDateTo || filters.createdBy || filters.order === 'oldest')
  const historyQuery = useQuery({ queryKey: ['documents', 'history', page, requestFilters], queryFn: () => getDocumentHistory(page, requestFilters), placeholderData: (previous) => previous })
  const errorText = historyQuery.error instanceof ApiError ? historyQuery.error.message : 'No pudimos cargar el historial. Intentá nuevamente.'

  useEffect(() => { setPage(0) }, [deferredQuery, filters.issueDateFrom, filters.issueDateTo, filters.createdBy, filters.order])
  const updateFilters = (next: Partial<DocumentHistoryFilters>) => setFilters((current) => ({ ...current, ...next }))
  const clearFilters = () => setFilters(emptyFilters)
  const download = async (record: DocumentHistoryRecord) => {
    setDownloadingId(record.id); setDownloadError('')
    try { const result = await regenerateDocument(record.id); downloadDocument(result.blob, filenameFromHeaders(result.headers)) }
    catch { setDownloadError('No pudimos descargar el PDF. Intentá nuevamente.') }
    finally { setDownloadingId(null) }
  }

  return <section className="max-w-[82rem]">
    <header className="border-b border-slate-200 pb-6 sm:flex sm:items-end sm:justify-between sm:gap-8">
      <div><p className="typo-eyebrow text-blue-700">Documentos emitidos</p><h1 className="typo-display-xl mt-2 uppercase">Historial</h1><p className="typo-body-sm mt-3 max-w-xl text-slate-600">Buscá, filtrá y recuperá los documentos generados.</p></div>
      {historyQuery.data && <p aria-live="polite" className="typo-meta mt-4 tabular-nums text-slate-600 sm:mt-0">{historyQuery.data.totalElements} {historyQuery.data.totalElements === 1 ? 'documento' : 'documentos'}</p>}
    </header>
    <section aria-label="Buscar y filtrar historial" className="mt-6 border-y border-slate-200 py-4">
      <div className="flex items-end gap-3 lg:hidden"><div className="min-w-0 flex-1"><Label htmlFor="history-search">Buscar</Label><div className="relative mt-2"><Search className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-slate-500" size={18} aria-hidden="true" /><Input id="history-search" value={filters.q} onChange={(event) => updateFilters({ q: event.target.value })} placeholder="Número, empresa o delegado" className="pl-10" /></div></div><Button variant="outline" className="min-h-12 shrink-0 px-3" aria-expanded={filtersOpen} onClick={() => setFiltersOpen(true)}><SlidersHorizontal size={18} aria-hidden="true" /> Filtros</Button></div>
      <div className="hidden gap-3 lg:grid lg:grid-cols-[minmax(16rem,1.6fr)_repeat(3,minmax(10rem,1fr))_auto] lg:items-end">
        <div className="hidden lg:block"><Label htmlFor="history-search-desktop">Buscar</Label><div className="relative mt-2"><Search className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-slate-500" size={18} aria-hidden="true" /><Input id="history-search-desktop" value={filters.q} onChange={(event) => updateFilters({ q: event.target.value })} placeholder="Número, empresa o delegado" className="pl-10" /></div></div>
        <div><Label htmlFor="history-from">Fecha desde</Label><Input id="history-from" className="mt-2" type="date" value={filters.issueDateFrom} onChange={(event) => updateFilters({ issueDateFrom: event.target.value })} /></div>
        <div><Label htmlFor="history-to">Fecha hasta</Label><Input id="history-to" className="mt-2" type="date" value={filters.issueDateTo} onChange={(event) => updateFilters({ issueDateTo: event.target.value })} /></div>
        {role === 'ADMIN' && <div><Label htmlFor="history-created-by">Generado por</Label><Input id="history-created-by" className="mt-2" value={filters.createdBy} onChange={(event) => updateFilters({ createdBy: event.target.value })} placeholder="Nombre" /></div>}
        <div><Label htmlFor="history-order">Orden</Label><Select id="history-order" className="mt-2" value={filters.order} onChange={(event) => updateFilters({ order: event.target.value as DocumentHistoryFilters['order'] })}><option value="newest">Más recientes</option><option value="oldest">Más antiguos</option></Select></div>
      </div>
      {hasFilters && <div className="mt-3 flex items-center justify-between gap-3"><p className="typo-meta flex items-center gap-2 text-slate-600"><SlidersHorizontal size={15} aria-hidden="true" /> Filtros activos</p><Button variant="ghost" className="min-h-9 px-2" onClick={() => { clearFilters(); setFiltersOpen(false) }}><X size={15} aria-hidden="true" /> Limpiar filtros</Button></div>}
    </section>
    {downloadError && <p role="alert" className="mt-4 border border-rose-200 bg-rose-50 p-4 text-sm text-rose-800">{downloadError}</p>}
    {historyQuery.isPending ? <LoadingState /> : historyQuery.isError ? <ErrorState text={errorText} onRetry={() => historyQuery.refetch()} /> : historyQuery.data.content.length === 0 ? <EmptyState role={role} filtered={hasFilters} onClear={clearFilters} /> : <>
      <div className="mt-6 hidden overflow-hidden border border-slate-200 bg-white md:block"><Table><TableHeader><TableRow><TableHead>Número</TableHead><TableHead>Generación</TableHead><TableHead>Tipo</TableHead><TableHead>Empresa</TableHead><TableHead>Delegado</TableHead><TableHead>Fecha permiso</TableHead><TableHead className="text-right">Acciones</TableHead></TableRow></TableHeader><TableBody>{historyQuery.data.content.map((record) => <HistoryRow key={record.id} record={record} downloading={downloadingId === record.id} onDetail={() => setSelected(record)} onDownload={() => download(record)} />)}</TableBody></Table></div>
      <div className="mt-5 divide-y divide-slate-200 border-y border-slate-200 md:hidden">{historyQuery.data.content.map((record) => <HistoryItem key={record.id} record={record} downloading={downloadingId === record.id} onDetail={() => setSelected(record)} onDownload={() => download(record)} />)}</div>
      <Pagination page={page} totalPages={historyQuery.data.totalPages} disabled={historyQuery.isFetching} onPageChange={setPage} />
    </>}
    <Dialog open={Boolean(selected)} onOpenChange={(open) => !open && setSelected(null)}>{selected && <DialogContent><DialogHeader><DialogTitle>{selected.publicNumber}</DialogTitle><DialogDescription>{documentTypeLabels[selected.documentType] ?? selected.documentType}</DialogDescription></DialogHeader><dl className="mt-6 grid grid-cols-[auto_1fr] gap-x-5 gap-y-4 text-sm"><Detail label="Generado por" value={selected.createdBy} /><Detail label="Generación" value={formatDateTime(selected.createdAt)} /><Detail label="Empresa" value={selected.companyName} /><Detail label="Delegado" value={selected.delegateName} /><Detail label="Fecha del permiso" value={formatDate(selected.issueDate)} /></dl><div className="mt-7 flex justify-end gap-2"><DialogClose asChild><Button variant="outline">Cerrar</Button></DialogClose><Button onClick={() => download(selected)} disabled={downloadingId === selected.id}>{downloadingId === selected.id ? <LoaderCircle className="animate-spin" size={17} /> : <Download size={17} />} Descargar PDF</Button></div></DialogContent>}</Dialog>
    <Dialog open={filtersOpen} onOpenChange={setFiltersOpen}><DialogContent className="bottom-sheet bottom-0 left-0 top-auto max-h-[78dvh] w-full max-w-none translate-x-0 translate-y-0 overflow-y-auto rounded-b-none rounded-t-2xl border-x-0 border-b-0 px-5 pb-[max(1.5rem,env(safe-area-inset-bottom))] pt-5 sm:max-w-lg lg:hidden"><DialogHeader><DialogTitle>Filtrar historial</DialogTitle><DialogDescription>Acotá los documentos que querés consultar.</DialogDescription></DialogHeader><div className="mt-6 grid gap-4"><div><Label htmlFor="history-from-mobile">Fecha desde</Label><Input id="history-from-mobile" className="mt-2" type="date" value={filters.issueDateFrom} onChange={(event) => updateFilters({ issueDateFrom: event.target.value })} /></div><div><Label htmlFor="history-to-mobile">Fecha hasta</Label><Input id="history-to-mobile" className="mt-2" type="date" value={filters.issueDateTo} onChange={(event) => updateFilters({ issueDateFrom: event.target.value })} /></div>{role === 'ADMIN' && <div><Label htmlFor="history-created-by-mobile">Generado por</Label><Input id="history-created-by-mobile" className="mt-2" value={filters.createdBy} onChange={(event) => updateFilters({ createdBy: event.target.value })} placeholder="Nombre" /></div>}<div><Label htmlFor="history-order-mobile">Orden</Label><Select id="history-order-mobile" className="mt-2" value={filters.order} onChange={(event) => updateFilters({ order: event.target.value as DocumentHistoryFilters['order'] })}><option value="newest">Más recientes</option><option value="oldest">Más antiguos</option></Select></div></div><div className="mt-7 flex justify-end gap-3"><DialogClose asChild><Button variant="outline">Cancelar</Button></DialogClose><DialogClose asChild><Button>Ver resultados</Button></DialogClose></div></DialogContent></Dialog>
  </section>
}

function HistoryRow({ record, downloading, onDetail, onDownload }: { record: DocumentHistoryRecord; downloading: boolean; onDetail: () => void; onDownload: () => void }) { return <TableRow><TableCell><button type="button" className="font-bold text-blue-800 underline-offset-4 hover:underline" onClick={onDetail}>{record.publicNumber}</button></TableCell><TableCell>{formatDateTime(record.createdAt)}</TableCell><TableCell><Badge>{documentTypeLabels[record.documentType] ?? record.documentType}</Badge></TableCell><TableCell>{record.companyName}</TableCell><TableCell>{record.delegateName}</TableCell><TableCell>{formatDate(record.issueDate)}</TableCell><TableCell className="text-right"><ActionGroup aria-label={`Acciones de ${record.publicNumber}`} className="ml-auto max-w-md"><ActionGroupItem><Button className="w-full" variant="outline" onClick={onDownload} disabled={downloading}>{downloading ? <LoaderCircle className="animate-spin" size={17} /> : <Download size={17} />} Descargar</Button></ActionGroupItem><ActionGroupItem><DisabledEmailAction label="Enviar por mail" /></ActionGroupItem></ActionGroup></TableCell></TableRow> }
function HistoryItem({ record, downloading, onDetail, onDownload }: { record: DocumentHistoryRecord; downloading: boolean; onDetail: () => void; onDownload: () => void }) { return <article className="bg-[#fffdf8] py-4 first:pt-0 last:pb-0"><div className="flex items-start justify-between gap-3"><div><button type="button" className="typo-control min-h-11 text-blue-800 underline-offset-4 hover:underline" onClick={onDetail}>{record.publicNumber}</button><Badge className="ml-2">{documentTypeLabels[record.documentType] ?? record.documentType}</Badge><p className="typo-label mt-1 text-slate-800">{record.companyName}</p><p className="typo-body-sm mt-0.5 text-slate-600">{record.delegateName} · {formatDate(record.issueDate)}</p></div><button type="button" aria-label={`Ver detalle de ${record.publicNumber}`} className="grid min-h-11 min-w-11 place-items-center text-slate-500 hover:bg-blue-50" onClick={onDetail}><FileText size={19} /></button></div><div className="mt-3 flex items-center justify-between gap-3 border-t border-slate-100 pt-3"><span className="typo-meta text-slate-500">{formatDateTime(record.createdAt)}</span><Button className="min-h-10 px-3" variant="outline" onClick={onDownload} disabled={downloading}>{downloading ? <LoaderCircle className="animate-spin" size={16} /> : <Download size={16} />} Descargar</Button></div></article> }
function Detail({ label, value }: { label: string; value: string }) { return <><dt className="font-semibold text-slate-500">{label}</dt><dd className="text-right text-slate-900">{value}</dd></> }
function Pagination({ page, totalPages, disabled, onPageChange }: { page: number; totalPages: number; disabled: boolean; onPageChange: (page: number) => void }) { return <div className="mt-5 flex items-center justify-between gap-3"><Button variant="outline" onClick={() => onPageChange(page - 1)} disabled={page === 0 || disabled}><ChevronLeft size={17} /> Anterior</Button><span className="text-sm font-semibold text-slate-600">Página {page + 1} de {Math.max(totalPages, 1)}</span><Button variant="outline" onClick={() => onPageChange(page + 1)} disabled={page + 1 >= totalPages || disabled}>Siguiente <ChevronRight size={17} /></Button></div> }
function LoadingState() { return <div className="mt-6 space-y-3">{[1, 2, 3].map((item) => <Skeleton key={item} className="h-24 w-full rounded-none" />)}</div> }
function EmptyState({ role, filtered, onClear }: { role: 'ADMIN' | 'DELEGADO'; filtered: boolean; onClear: () => void }) { return <div className="mt-6 border border-dashed border-slate-300 bg-[#fffdf8] p-10 text-center"><FileText className="mx-auto text-slate-400" /><h2 className="mt-4 text-2xl">{filtered ? 'No encontramos documentos con esos filtros.' : 'No hay documentos generados todavía.'}</h2><p className="mt-2 text-sm text-slate-600">{filtered ? 'Probá con otro número, fechas más amplias o limpiá los filtros.' : role === 'DELEGADO' ? 'Los documentos que generes aparecerán acá.' : 'Los documentos generados aparecerán acá.'}</p>{filtered && <Button variant="outline" className="mt-5" onClick={onClear}>Limpiar filtros</Button>}</div> }
function ErrorState({ text, onRetry }: { text: string; onRetry: () => void }) { return <div role="alert" className="mt-6 border border-rose-200 bg-rose-50 p-6"><div className="flex gap-3 text-rose-900"><AlertCircle className="shrink-0" size={20} /><p className="font-semibold">{text}</p></div><Button variant="outline" className="mt-4 border-rose-300 text-rose-900" onClick={onRetry}>Reintentar</Button></div> }
function formatDate(date: string) { return new Intl.DateTimeFormat('es-AR', { dateStyle: 'short' }).format(new Date(`${date}T12:00:00`)) }
function formatDateTime(date: string) { return new Intl.DateTimeFormat('es-AR', { dateStyle: 'short', timeStyle: 'short' }).format(new Date(date)) }
